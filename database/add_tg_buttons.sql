-- ============================================================
-- Миграция: добавление поддержки кастомных кнопок в TG постах
-- Запускать: psql $DATABASE_URL -f database/add_tg_buttons.sql
-- Идемпотентен: безопасно запускать повторно.
-- ============================================================

-- Кнопки для запланированных задач автоплана
-- NULL = дефолтная кнопка "Смотреть товар" (обратная совместимость)
-- JSONB = массив рядов [[{text, type, url?, filter?}], ...]
ALTER TABLE tg_scheduled
  ADD COLUMN IF NOT EXISTS buttons JSONB;
